# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Site
NEXT_PUBLIC_SITE_URL=

# Delivery policy: JSON with "*" for one flat rate, or keys such as
# "bairro:centro" and "cep:12020100". Values are BRL numbers; use 0 only
# for explicitly free delivery. Delivery checkout stays blocked if unmatched.
NEXT_PUBLIC_DELIVERY_FEES={}
